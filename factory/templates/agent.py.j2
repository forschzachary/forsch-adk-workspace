"""{{ a.adk_name }} agent definition (generated by the Forsch ADK Factory — do not hand-edit; edit agent_specs/agents.yaml)."""

from __future__ import annotations

import os

from google.adk import Agent
from google.adk.models.lite_llm import LiteLlm
{% if tool_leaves %}from forsch.adk_components.tools import (
{% for leaf in tool_leaves %}    {{ leaf }},
{% endfor %})
{% endif %}

_LITELLM_BASE_URL = os.environ.get("LITELLM_BASE_URL", "http://127.0.0.1:4000/v1")
_LITELLM_API_KEY = (
    os.environ.get("ADK_LITELLM_KEY_{{ a.id|upper }}")  # per-agent key → per-agent LiteLLM analytics
    or os.environ.get("LITELLM_HERMES_KEY")             # shared fallback (never breaks if the per-agent key is absent)
    or os.environ.get("LITELLM_MASTER_KEY")
    or os.environ.get("LITELLM_API_KEY")
)
# Model: a per-agent pin hard-wins; unpinned agents fall to FORSCH_ADK_MODEL. LiteLLM owns routing + fallback.
_LITELLM_MODEL = {{ model_expr }}

{{ a.id }}_model = LiteLlm(
    model=_LITELLM_MODEL,
    api_base=_LITELLM_BASE_URL,
    api_key=_LITELLM_API_KEY,
)

root_agent = Agent(
    name={{ name_repr }},
    model={{ a.id }}_model,
    description={{ description_repr }},
    instruction={{ instruction_repr }},
    tools=[
{% for leaf in tool_leaves %}        {{ leaf }},
{% endfor %}    ],
)

agent = root_agent
