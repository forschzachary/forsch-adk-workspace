# Agent specs directory note

Purpose: source-of-truth manifests for ADK agents.

`agents.yaml` describes each agent's runtime package, ADK Web wrapper, Discord channel routes, safety level, tools, and smoke prompts. Generated code should flow from this manifest rather than drifting across hand-edited files.
