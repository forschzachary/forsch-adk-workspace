# Stability web wrapper directory note

Purpose: ADK Web entrypoint for the packaged stability agent.

`agent.py` imports the real runtime agent from `agents/stability`. `root_agent.yaml` mirrors it so ADK Web has an editable surface for collaborative graph design.
