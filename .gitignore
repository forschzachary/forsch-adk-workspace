# Generated state
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
.env
.DS_Store
*.db
*.sqlite
*.sqlite3


# Household runtime data (personal — grocery log + reminders; never track)
data/household/

# ADK web/session scratch
web_agents/**/.adk/
web_agents/**/__pycache__/
chat/.venv/
chat/chat.env
chat/hubert_soul.md
*.egg-info/
.chainlit/
.claude/settings.local.json
*.pyc

# Cockpit theme + CSS are source, keep them tracked
!chat/public/
!chat/public/**

# local ADK web gateway creds (never commit)
.adk-local.env

# forsch chat persistent sessions
.forsch/

# forsch /goal run-state ledgers
goals/

# Huberto per-friend memory (local)
data/friends/

# Convo-mirror per-friend thread map (local; discord ids + thread ids)
data/mirror/

# Deploy secrets — only the *.example templates are tracked (never the filled files)
deploy/screeningroom.env
deploy/secrets/
deploy/cli.json
data/audit.jsonl
