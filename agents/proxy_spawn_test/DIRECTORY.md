# Proxy Spawn Test agent directory note

Purpose: independent ADK repo for the proxy_spawn_test agent.

Structure:

- `pyproject.toml` - package metadata for `forsch-agent-proxy_spawn_test`.
- `src/forsch/agent_proxy_spawn_test/agent.py` - ADK Agent definition.
- `tests/` - unit tests.
- `README.md` - setup/run notes.

Git ownership: this directory is the `forschzachary/forsch-agent-proxy_spawn_test` repo.
