# website agent

Forsch ADK team lead for Zach's personal branded website launch.

This agent plans and audits launch work only. It does not deploy, change DNS,
publish pages, or change newsletter/booking provider settings.

## Setup

```bash
uv pip install -e .
```

## Run

```bash
python -m forsch.agent_website.agent
```
