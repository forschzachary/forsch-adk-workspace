# Test Ui Spawn agent directory note

Purpose: independent ADK repo for the test_ui_spawn agent.

Structure:

- `pyproject.toml` - package metadata for `forsch-agent-test_ui_spawn`.
- `src/forsch/agent_test_ui_spawn/agent.py` - ADK Agent definition.
- `tests/` - unit tests.
- `README.md` - setup/run notes.

Git ownership: this directory is the `forschzachary/forsch-agent-test_ui_spawn` repo.
