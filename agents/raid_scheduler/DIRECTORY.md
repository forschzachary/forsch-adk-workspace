# Raid Scheduler agent directory note

Purpose: independent ADK repo for the raid_scheduler agent.

Structure:

- `pyproject.toml` - package metadata for `forsch-agent-raid_scheduler`.
- `src/forsch/agent_raid_scheduler/agent.py` - ADK Agent definition.
- `tests/` - unit tests.
- `README.md` - setup/run notes.

Git ownership: this directory is the `forschzachary/forsch-agent-raid_scheduler` repo.
